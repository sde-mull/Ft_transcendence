class History
{
    constructor(pos, theta, radius)
    {
        this.pos = pos;
        this.truepos = [pos[0] + width / 2, pos[1] + height / 2];
        this.theta = theta;
        this.radius = radius;
    }
}